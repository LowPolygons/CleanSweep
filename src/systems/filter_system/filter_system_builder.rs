use crate::systems::filter_system::filter_system::FilterSystem;

// TODO: Decouple this plan eventually
pub fn construct(filter_system: &mut FilterSystem) {
    // Read in whatever identifies what filter categories should be used
    // Construct them, feeding in the user settings
    // the filter_system will now be ready for use
}
